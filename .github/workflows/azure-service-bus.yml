name: azure-service-bus

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/test-template.yml
    with:
      TARGET: CIAzureServiceBus
